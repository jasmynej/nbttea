import * as React from "react"
import Layout from "../../../components/layout"
import { graphql } from "gatsby"

export default function ProductTypePage({data,pageContext}){
    return(
        <Layout>
            <div>

                \
            </div>
        </Layout>
    )
}